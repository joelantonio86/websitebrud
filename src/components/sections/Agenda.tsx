const Agenda: React.FC = () => {
  return (
    <section id="agenda" className="section">
      <div className="container">
        <h2 className="section-title">Agenda</h2>
        <p>Conteúdo da agenda será migrado aqui...</p>
      </div>
    </section>
  );
};

export default Agenda;
